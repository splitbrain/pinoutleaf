{
  "name": "pinoutleaf",
  "description": "Generate pinout leaves for electronics",
  "license": "MIT",
  "author": "Andreas Gohr",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  },
  "dependencies": {
    "svgdom": "^0.1.21"
  }
}
