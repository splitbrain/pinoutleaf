const PINSIZE = 60; // 0.6mm The size of a pin hole
const PINSPACE = 254; // 2.54mm The space between pin holes (standard 0.1 inch)
const PADDING = 100; // 1mm Padding between labels
const FONTSIZE = 150; // Font size for labels

export {
    PINSIZE,
    PINSPACE,
    PADDING,
    FONTSIZE
}
